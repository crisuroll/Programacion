package ejemploClase;

/**
 * Clase ejemploClase. Apuntes UD01 Programacion. Este es un comentario Javadoc.
 * Aqui debe aparecer informacion acerca del proyecto. Etiquetas explicadas en apuntes.
 */

// Importacion de paquetes

import java.util.Scanner;

public class ejemploClase {

	/**
	 * Metodo main.
	 * @param args
	 */
	
	public static void main(String[] args) {

		/* Los proyectos creados se organizan en carpetas y workspaces. 
		 * Hilo de ejecucion: secuencia simple de instrucciones ejecutada en paralelo con
		 * otros hilos.
		 * Paquetes con nombre. Las clases se llaman igual que sus ficheros.
		 * Parametro: la variable en la declaración del método.
		 * Argumento: valor enviado al parametro.
		 * Cabecera de un metodo: indica los parametros que recibe el metodo.
		 * Ambito: fragmento de codigo en el que se puede utilizar una variable.
		 * Metodo main: punto de entrada de todo programa.
		 * Ficheros codigo fuente: .java ; ficheros compilados: .class
		 * Cada fichero debe contener solo una clase java (consejo).
		 * Metodos separados por una linea en blanco. Deben ser verbos.
		 * Hay que cuidar la tabulacion y los espacios en blanco
		 */
		
		/* Todo proyecto debe tener una calidad basada en normas claras y concisas a la hora
		 * de programar. En este archivo vamos a recopilar algunas de ellas con ejemplos.
		 */
		
		// VARIABLES. Se declara una por linea y se inicializan al declararse.
		int num1 = 5;
		int a = 1;
		int b = 1;
		int c = 1;
		int d = 1;
		
		// CONSTANTES. Variable cuyo valor nunca cambia.
		final int DIAS_SEMANA = 7;
		
		/* Tanto metodos, clases, interfaces y metodos se escribe en minuscula, y las
		 * siguientes palabras se escriben en mayusculas separadas por _
		 */
		
		if ((a == b) && (c == d)) {
			
			// Las condiciones siempre van entre parentesis
		}
			
		/*
		 * Comentario de bloque
		 */
			
		/* Comentario de linea */
		
		// Comentario corto
		
		
	}

}
